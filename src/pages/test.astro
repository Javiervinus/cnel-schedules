---
import MainLayout from "@/layouts/MainLayout.astro";
import { getFeatureFlag } from "@/utils/feature-flags";

export const prerender = false;
const featureCookies = Astro.cookies.get("vercel-flag-overrides")?.value;
console.log("🚀 ~ constGET:APIRoute= ~ featureCookies:", featureCookies);

const flagValue = await getFeatureFlag("newScheduleAvailable", featureCookies);
---

<MainLayout title="testingPage">
  <code>
    <pre>{JSON.stringify(featureCookies)}</pre>

    <pre>{JSON.stringify(flagValue)}</pre>
  </code>
</MainLayout>
